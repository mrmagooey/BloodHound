<!-- Documentation: https://agents.md -->

# AGENTS.md

## Architecture

BloodHound has two operating modes:

- **Standard mode**: Neo4j (graph) + PostgreSQL (application data), with authentication and UI
- **Standalone mode**: kglite (embedded Rust graph DB) + SQLite, no auth, single binary

kglite is a Rust-based embedded graph database that supports a subset of Cypher. It lives at `kglite-ffi/` as a git submodule and is linked into the Go binary via CGO as a static library (`libkglite.a`).

The DAWGS abstraction layer (`packages/go/kglite/dawgs/`) implements the same `graph.Database`, `graph.Transaction`, and `graph.Batch` interfaces as the Neo4j driver from `github.com/specterops/dawgs`. This lets analysis code run against either backend without changes.

Cypher queries generated by the Neo4j query builder pass through Go-side rewrites (`rewriteForKglite` in `transaction.go`) before reaching kglite's Rust parser/optimizer/executor. The rewrite layer handles three incompatibilities: label checks in WHERE clauses, pipe-separated relationship types, and parameterized IN lists.

## Key directories

| Path | Description |
|------|-------------|
| `kglite-ffi/` | Rust source (git submodule). Builds `libkglite.a`. Contains Cypher parser, optimizer, executor |
| `kglite-ffi/kglite/` | Core Rust graph engine crate |
| `kglite-ffi/src/ffi.rs` | C FFI boundary called from Go |
| `packages/go/kglite/kglite.go` | CGO bridge - Go functions calling into `libkglite.a` via C |
| `packages/go/kglite/kglite.h` | C header for the FFI |
| `packages/go/kglite/dawgs/` | DAWGS driver: `driver.go` (Database), `transaction.go` (Transaction + Cypher rewriting), `batch.go` (Batch with edge dedup + bulk FFI), `profiler.go` (query timing), `query.go`, `result.go` |
| `packages/go/analysis/azure/` | Azure analysis, including batched role assignment (`role.go`) and post-processing (`post.go`) |
| `cmd/api/src/services/graphify/` | Ingestion pipeline: JSON parsing, node/relationship conversion, ingest orchestration |
| `cmd/api/src/services/graphify/endpoint/` | Endpoint resolver: resolves edge endpoints against the database before MERGE (caches results, drops unresolved edges) |
| `cmd/api/src/services/graphify/ingestrelationships.go` | `endpointIdentityKind` — determines the MERGE label for relationship endpoint stubs (Base, AZBase, or sourceKind for OpenGraph) |
| `cmd/api/src/cmd/bhapi/` | API entry point (`main.go`) |
| `cmd/api/src/test/e2e/` | End-to-end tests, comparison tests (kglite vs Neo4j), golden tests, DAWGS driver tests |
| `cmd/api/src/test/e2e/repro/` | Reproduction tests for specific kglite/Neo4j divergences |
| `cmd/api/src/test/e2e/testdata/golden/` | Golden reference files generated from Neo4j (source of truth) |
| `dockerfiles/standalone.Dockerfile` | Multi-stage build: Rust -> Go -> Debian slim runtime |
| `.github/workflows/standalone.yml` | CI for standalone mode |

## Build instructions

### Prerequisites
- Go 1.25.0+
- Rust stable toolchain (for building kglite)
- GCC / libc6-dev (CGO needs a C linker)
- Git submodules checked out: `git submodule update --init --recursive`

### Build kglite static library
```sh
cd kglite-ffi && cargo build --release --no-default-features --features ffi
```
This produces `kglite-ffi/target/release/libkglite.a`. **You must rebuild this after any changes to Rust source files.**

### Build standalone binary
```sh
CGO_ENABLED=1 go build -tags standalone -o bloodhound-standalone ./cmd/api/src/cmd/bhapi
```

### Build container
```sh
docker build -f dockerfiles/standalone.Dockerfile -t bloodhound-standalone .
```

## Testing

The test suite lives in `cmd/api/src/test/e2e/` and validates that kglite produces identical results to Neo4j. Tests are gated by build tags: `e2e` for all end-to-end tests, `comparison` for tests that additionally require a live Neo4j instance.

### E2E tests (no Neo4j needed)
Ingest sample datasets (AD, Azure, KNexus) into kglite, run attack-path analysis, and validate node/edge counts and query results.
```sh
make test          # full e2e suite
make test-quick    # smoke test (Azure attack path analysis only)
```
Set `BH_SKIP_KNEXUS=1` or `BH_SKIP_COMBINED=1` to skip loading expensive fixtures during development.

### Golden tests
Compare kglite query results against pre-generated reference files. **Neo4j is the source of truth** — golden files are generated by running queries against Neo4j and saving the results. kglite is expected to match these results exactly. Golden files cover AD (49 queries), Azure (14 queries), KNexus (53 queries), and KNexus OpenGraph (305 queries). Stored in `cmd/api/src/test/e2e/testdata/golden/`.

When kglite behavior changes, mismatches should be investigated against Neo4j before updating golden files.
```sh
make golden-test       # compare kglite against golden files (AD + Azure)
make golden-test-all   # include KNexus datasets

# regenerate golden files from Neo4j (requires Docker)
docker compose -f docker-compose.testing.yml up -d
make golden-generate
docker compose -f docker-compose.testing.yml down
```

### Comparison tests (requires Neo4j)
Run the same queries against both kglite and a live Neo4j instance side-by-side, reporting matches/mismatches with timing.
```sh
docker compose -f docker-compose.testing.yml up -d
make test-comparison   # kglite vs Neo4j (AD, Azure, KNexus)
make test-adminer      # AD_Miner query compatibility
docker compose -f docker-compose.testing.yml down
```

### Reproduction tests
Isolated tests in `cmd/api/src/test/e2e/repro/` targeting specific kglite/Neo4j behavioral divergences: MERGE label isolation, idempotency across batch flushes, cross-platform stub deduplication, and batch flush boundary behavior.
```sh
go test -v -tags e2e -timeout 5m ./cmd/api/src/test/e2e/repro/
```

### Rust tests (kglite engine)
```sh
make test-rust
```

### Make target reference

| Target | Neo4j Required | Description |
|---|---|---|
| `test` | No | Full e2e suite (ingest + analysis + queries) |
| `test-quick` | No | Smoke test (Azure attack paths only) |
| `golden-test` | No | kglite vs golden files (AD + Azure) |
| `golden-test-all` | No | kglite vs golden files (all datasets) |
| `golden-generate` | Yes | Generate golden files from Neo4j |
| `test-comparison` | Yes | Side-by-side kglite vs Neo4j |
| `test-adminer` | Yes | AD_Miner query compatibility |
| `test-knexus` | Yes | KNexus dataset comparison |
| `test-rust` | No | kglite Rust unit tests |

## Build tags

| Tag | Purpose |
|-----|---------|
| `standalone` | Builds without UI assets, uses kglite+SQLite instead of Neo4j+PostgreSQL |
| `e2e` | Enables end-to-end test files |
| `comparison` | Enables kglite-vs-Neo4j comparison tests (requires Neo4j) |
| `integration` | Standard BloodHound integration tests |

## Common patterns

### Cypher rewriting (`transaction.go`)
The `rewriteForKglite` function in `packages/go/kglite/dawgs/transaction.go` applies regex-based transforms:
- `rewriteMultiTypeRel`: Converts `[:TypeA|TypeB]` to `WHERE type(r) IN [...]`
- `rewriteLabelWhere`: Converts `n:Kind` in WHERE to `hasLabel(n, 'Kind')`
- `rewriteInParam`: Expands `IN $param` to `IN [literal, ...]`
- `rewriteEmptyWhere`: Strips empty WHERE clauses left after other rewrites
- `rewriteStructural`: Cached structural rewrites applied before parameter-dependent transforms

### Batch operations (`batch.go`)
`packages/go/kglite/dawgs/batch.go` buffers operations and flushes in bulk. Default flush size: 5000 for both Cypher operations and edge specs.

Relationship creation has two paths:
- **Fast path (FFI)**: When both endpoint objectids are in the `oidToIdx` cache with matching labels, edges are created via bulk `CreateEdgesBatch` FFI — no Cypher parsing overhead.
- **Slow path (triple-MERGE)**: When cache misses, falls back to `MERGE (s:Label {oid}) MERGE (e:Label {oid}) MERGE (s)-[r:Type]->(e)` via Cypher.

The `oidToIdx` cache is label-aware: `map[objectid]map[label]nodeIndex`. After each Cypher flush, `resolvePendingLookups` populates the cache so subsequent operations can use the fast path. The `kindsWritten` map tracks which objectids have had their `__kinds` property set to prevent stubs from overwriting authoritative node data.

### Profiler (`profiler.go`)
`packages/go/kglite/dawgs/profiler.go` provides `QueryProfiler` for per-query timing. Enable with `EnableProfiling()`, retrieve with `Report()`. Normalizes queries (strips IDs/params) for aggregation.

### CGO bridge (`kglite.go`)
`packages/go/kglite/kglite.go` wraps C FFI calls. Key types: `KnowledgeGraph` (main handle), exposed via `New()`, `Load()`, `Save()`, `Cypher()`, `CypherBatch()`.

## Known limitations

- kglite supports a Cypher subset, not full Neo4j Cypher. The Go rewrite layer bridges some gaps.
- Variable-length path patterns with multi-type relationships (`[r:Type1|Type2*]`) cannot be rewritten and must use single types.
- The Rust optimizer handles predicate pushdown for `type(r)` and `id(e)` expressions.
- MERGE with secondary labels has specific handling (see commit history).
- No APOC procedures - analysis code must use plain Cypher or Go-side logic.
- **No transaction isolation**: kglite's `ReadTransaction` and `BatchOperation` share the same underlying `KnowledgeGraph` instance. After a batch `flush()`, writes are immediately visible to concurrent `ReadTransaction` calls. This differs from Neo4j, where batch writes are only visible after commit. This affects the endpoint resolver during ingestion — kglite's resolver can see nodes created by earlier files in the same batch, while Neo4j's cannot.

## Golang code standards
- Receiver functions on structs should prefer `s` as the variable name instead of the first letter of the struct type.
- Named returns are not allowed; all return variables must be defined in the function.
- Group variable initializations in a `var ( ... )` block and hoist them to the top of the function when possible.
- Use `any` instead of `interface{}`.
- Prefer rich variable names, for example: `databaseInterface` instead of `di` or `dbi`.
- These standards apply to user/agent-written code, not auto-generated code (e.g., unit test mocks).

## Test code instructions for Golang
- If a test file tests internal (non-exported) logic, use the same package as the code being tested.
- If a test file only tests exported logic, use the package name with `_test` appended.
  - Example: `auth.go` has `package api`, `auth_test.go` testing exports should have `package api_test`
- Integration tests go in separate files (e.g., `auth_integration_test.go`) with a build tag: `//go:build integration` (or `serial_integration`, `slow_integration`).

## PR instructions
- Title format: `<conventional commit tag>: <Title> <Jira tag>`
- On the standalone branch, use `make test` and `make golden-test` before committing. On main, use `just prepare-for-codereview` if available.
- Code files must have a license header (see `LICENSE.header`).
- If API endpoints change, update OpenAPI yaml files and `openapi.json`.
