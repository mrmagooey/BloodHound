{
    "$schema": "https://turbo.build/schema.json",
    "tasks": {
        "build": {
            "dependsOn": ["^build"],
            "outputs": ["dist/**"],
            "inputs": ["src/**", "tsconfig*.json", "rollup.config.*", "vite.config.*", "package.json"]
        }
    }
}
